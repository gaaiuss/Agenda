# Configure Django

```
django-admin start-project project .
python manage.py startapp contact
```

- startproject: "project" is the standard name of the project, you can change it if you
want
- startapp: When creating an app, always update the installed apps in the project
settings.py
